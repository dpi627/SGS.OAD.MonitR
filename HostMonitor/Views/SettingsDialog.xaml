<UserControl x:Class="HostMonitor.Views.SettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Width="400">
    <StackPanel Margin="16">
        <TextBlock Text="設定"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16" />

        <TextBlock Text="監控間隔 (秒)" FontWeight="Bold" Margin="0,0,0,8" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Slider Value="{Binding MonitorIntervalSeconds}"
                    Minimum="{Binding MinInterval}"
                    Maximum="60"
                    Width="200"
                    TickFrequency="5"
                    IsSnapToTickEnabled="False"
                    VerticalAlignment="Center" />
            <TextBox Text="{Binding MonitorIntervalSeconds, UpdateSourceTrigger=PropertyChanged}"
                     Width="60"
                     Margin="16,0,0,0"
                     materialDesign:HintAssist.Hint="秒" />
        </StackPanel>

        <TextBlock Text="{Binding MonitorIntervalSeconds, StringFormat=每 {0} 秒檢查一次}"
                   FontSize="12"
                   Opacity="0.7"
                   Margin="0,0,0,16" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="取消"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0" />
            <Button Content="儲存"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </StackPanel>
</UserControl>
