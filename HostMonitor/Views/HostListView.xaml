<UserControl x:Class="HostMonitor.Views.HostListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:behaviors="clr-namespace:HostMonitor.Behaviors"
             xmlns:models="clr-namespace:HostMonitor.Models.Enums"
             xmlns:controls="clr-namespace:HostMonitor.Controls"
             mc:Ignorable="d">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="新增主機"
                    Command="{Binding AddHostCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
            <TextBox materialDesign:HintAssist.Hint="搜尋主機..."
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="200"
                     Margin="16,0" />
        </StackPanel>

        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Hosts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="0,0,0,8" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" MinWidth="200" />
                                        <ColumnDefinition Width="2.5*" MinWidth="200" />
                                        <ColumnDefinition Width="3*" MinWidth="250" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- 第一欄：主機資訊 -->
                                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" />
                                        <TextBlock Text="{Binding DisplayAddress}" FontSize="14" Opacity="0.7" Margin="0,4,0,0" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <materialDesign:Chip Content="{Binding Type}" Margin="0,0,8,0" />
                                            <TextBlock Text="{Binding CurrentStatus}"
                                                       Foreground="{Binding CurrentStatus, Converter={StaticResource StatusToColorConverter}}"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding LastCheckTime, StringFormat=最後檢查: {0:yyyy/MM/dd HH:mm:ss}, TargetNullValue=最後檢查: 未檢查}"
                                                   FontSize="12"
                                                   Opacity="0.7"
                                                   Margin="0,8,0,0" />
                                        <TextBlock Text="{Binding AverageResponseTimeMs, StringFormat=平均回應: {0:N2} ms, TargetNullValue=平均回應: --}"
                                                   FontSize="12"
                                                   Opacity="0.7"
                                                   Margin="0,4,0,0" />
                                        <ProgressBar Height="4" Margin="0,8,0,0" IsIndeterminate="True">
                                            <ProgressBar.Style>
                                                <Style TargetType="ProgressBar">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CurrentStatus}" Value="{x:Static models:HostStatus.Checking}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ProgressBar.Style>
                                        </ProgressBar>
                                    </StackPanel>

                                    <!-- 第二欄：Mini Console -->
                                    <Border Grid.Column="1"
                                            Margin="0,0,16,0"
                                            Padding="8"
                                            Background="#0A000000"
                                            BorderBrush="#22000000"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ListBox ItemsSource="{Binding CommandLog}"
                                                 MaxHeight="120"
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                                 behaviors:AutoScrollBehavior.Enable="True">
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="Focusable" Value="False" />
                                                    <Setter Property="IsHitTestVisible" Value="False" />
                                                    <Setter Property="Padding" Value="0" />
                                                    <Setter Property="Margin" Value="0" />
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"
                                                               FontFamily="Consolas"
                                                               FontSize="11"
                                                               TextWrapping="Wrap" />
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>

                                    <!-- 第三欄：回應時間圖表 -->
                                    <Border Grid.Column="2"
                                            Margin="0,0,16,0"
                                            Background="#05000000"
                                            BorderBrush="#11000000"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <Grid>
                                            <controls:ResponseTimeChart Values="{Binding ResponseTimeHistory}"
                                                                         Height="100" />
                                            <TextBlock Text="回應時間 (ms)"
                                                       HorizontalAlignment="Left"
                                                       VerticalAlignment="Top"
                                                       Margin="8,4,0,0"
                                                       FontSize="10"
                                                       Opacity="0.5" />
                                            <TextBlock Text="{Binding ResponseTimeHistory.Count, StringFormat=({0})}"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Top"
                                                       Margin="0,4,8,0"
                                                       FontSize="10"
                                                       Opacity="0.5" />
                                        </Grid>
                                    </Border>

                                    <!-- 第四欄：功能按鈕 -->
                                    <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                        <Button Content="編輯"
                                                Command="{Binding DataContext.EditHostCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                Width="90"
                                                Margin="0,0,0,8" />
                                        <Button Content="刪除"
                                                Command="{Binding DataContext.DeleteHostCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                Width="90"
                                                Margin="0,0,0,8" />
                                        <Button Content="立即檢查"
                                                Command="{Binding DataContext.ManualCheckCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignRaisedButton}"
                                                Width="90" />
                                    </StackPanel>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Hosts.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="尚無主機" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" />
                <TextBlock Text="點擊下方按鈕新增第一台主機" Margin="0,0,0,16" />
                <Button Content="新增主機"
                        Command="{Binding AddHostCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
