<Window x:Class="HostMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="HostMonitor"
        Width="1200"
        Height="800"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <materialDesign:DialogHost Identifier="RootDialog">
        <DockPanel>
            <materialDesign:ColorZone DockPanel.Dock="Top" Mode="PrimaryMid" Padding="16">
                <DockPanel>
                    <TextBlock Text="HostMonitor" FontSize="22" VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="開始監控"
                                Command="{Binding StartMonitoringCommand}"
                                IsEnabled="{Binding IsMonitoring, Converter={StaticResource InverseBoolConverter}}"
                                Margin="8,0" />
                        <Button Content="停止監控"
                                Command="{Binding StopMonitoringCommand}"
                                IsEnabled="{Binding IsMonitoring}"
                                Margin="8,0" />
                    </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>

            <Border DockPanel.Dock="Bottom"
                    Background="#333333"
                    Padding="12,4"
                    TextElement.Foreground="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding AppVersion, StringFormat=版本: {0}}"
                               Margin="0,0,16,0" />
                    <TextBlock Grid.Column="1"
                               Text="{Binding MonitorHostCount, StringFormat=監控主機數量: {0}}"
                               Margin="0,0,16,0" />
                    <TextBlock Grid.Column="2"
                               Text="{Binding UserName, StringFormat=使用者: {0}}"
                               Margin="0,0,16,0" />
                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="監控:" Margin="0,0,6,0" />
                        <Ellipse Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#777777" />
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMonitoring}" Value="True">
                                            <Setter Property="Fill">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="White" />
                                                </Setter.Value>
                                            </Setter>
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="MonitoringBlink">
                                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                                        <ColorAnimation Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                                                        To="Black"
                                                                        Duration="0:0:0.6" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <StopStoryboard BeginStoryboardName="MonitoringBlink" />
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="未監控" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMonitoring}" Value="True">
                                            <Setter Property="Text" Value="監控中" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <materialDesign:Snackbar DockPanel.Dock="Bottom"
                                     MessageQueue="{Binding SnackbarMessageQueue}"
                                     Margin="16" />

            <ContentControl Content="{Binding HostListViewModel}" DockPanel.Dock="Top" />
        </DockPanel>
    </materialDesign:DialogHost>
</Window>
